dibujar graficamente los datos de la practica anterior

gnuplot
^  = **
plot -> dibujar
[x:y] -> rango

prac > ficherodatos.txt

plot "datos.txt" with linespoints

fit f(x) "d.dat" via a,b

print a, b

plot "d.dat" with linespoints, f(x)

gnuplot -persist

pause -1 "hasta pulsar retorno"

