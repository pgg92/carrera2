1.
T,	utiliza otras operadoras internas, utiliza la suma
	generadoras != derivada, nunca pueden serlo
2.
T

3.
T,	si el arbol es BB, si esta en el peor caso,
	degradado a lista, la altura es el nº de nodos

4.
F, devuelve el primero

5.
	devuelve el numero de nodos de la lista
	devuelve un nodo mas

6.
T	

7.
F

8.
F, IRD, RID, IDR, (DRI, RDI, DIR son los inversos especulares)

9.
F

10.
quita todas las hojas del arbol

11.
asig(vector, posicion, item) => vector(sintaxis)

si y = j entonces
	asig(asjg(v, i, x), j, y) => asig(v, i, y)
sino
	asig(asig(v, i, x) j, y) => asig(asig(v, j, y), i, x)

x. las operaciones constructoras generadoras tiene sintaxis pero no semantica
x. en un arbol binario completo todos los nodos hojas estan al mismo nivel
F
x. en un arbol binario lleno todos los nodos hojas estan al mismo nivel
T

